# Optional. The app runs without these by falling back to the bundled
# sample dataset (src/data/sample-robots.json). Set them to use Supabase.
#
# Copy this file to .env.local and fill in your project values.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
